# Simple StarkNet contract for deposit and withdrawal.

# Define the main program.
def main(inputs, outputs):
    # Read input values.
    action = inputs[0]
    amount = inputs[1]
    balance = inputs[2]

    # Perform action based on input.
    if action == 0:  # Deposit
        new_balance = balance + amount
    elif action == 1:  # Withdraw
        assert amount <= balance  # Ensure sufficient balance for withdrawal
        new_balance = balance - amount
    else:
        # Invalid action
        assert false

    # Write the new balance to the output.
    outputs[0] = new_balance

# Define the entry point for the program.
entry:
    # Three inputs (action, amount, balance) and one output (new_balance).
    3 -> main
