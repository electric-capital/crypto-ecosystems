# Simple Counter in Cairo.

# Define the main program.
def main(inputs, outputs):
    # Read input values.
    current_count = inputs[0]
    increment_by = inputs[1]

    # Increment the counter by the specified amount.
    new_count = current_count + increment_by

    # Write the new counter value to the output.
    outputs[0] = new_count

# Define the entry point for the program.
entry:
    # Two inputs (current_count, increment_by) and one output (new_count).
    2 -> main
