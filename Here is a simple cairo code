# Simple Cairo program to add two numbers.

# Define the main program.
def main(inputs, outputs):
    # Read input values.
    a = inputs[0]
    b = inputs[1]

    # Perform addition.
    result = a + b

    # Write the result to the output.
    outputs[0] = result

# Define the entry point for the program.
entry:
    # Two inputs (a and b) and one output (result).
    2 -> main
