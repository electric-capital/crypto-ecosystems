# Iterative Fibonacci Calculation in Cairo.

# Define the main program.
def main(inputs, outputs):
    # Read input value.
    n = inputs[0]

    # Calculate Fibonacci sequence using the iterative function.
    result = fibonacci(n)

    # Write the result to the output.
    outputs[0] = result

# Define the iterative Fibonacci function.
def fibonacci(x):
    if x == 0:
        return 0
    elif x == 1:
        return 1
    else:
        a, b = 0, 1
        for _ in range(x - 1):
            a, b = b, a + b
        return b

# Define the entry point for the program.
entry:
    # One input (n) and one output (result).
    1 -> main
